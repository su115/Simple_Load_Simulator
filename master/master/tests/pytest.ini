[pytest]
addopts = --maxfail=1
markers =
    network: marks tests that interact with network or nginx server
    testclass: test class simple tests
    integration: integration tests
    slow: slow tests

